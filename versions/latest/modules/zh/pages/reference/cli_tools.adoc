= CLI 工具
:page-languages: [en, zh]
:revdate: 2025-08-21
:page-revdate: {revdate}

RKE2 提供了多个 CLI 工具，以帮助访问和调试集群。在启动时，它们被提取到 `/var/lib/rancher/rke2/bin`。

== kubectl

管理员 kubeconfig 会在 `/etc/rancher/rke2/rke2.yaml` 生成。

示例：

[,bash]
----
export KUBECONFIG=/etc/rancher/rke2/rke2.yaml
/var/lib/rancher/rke2/bin/kubectl get nodes
----

== Containerd

RKE2 附带 `ctr` 和 `crictl`。Containerd 套接字位于 `/run/k3s/containerd/containerd.sock`。

示例：

[,bash]
----
/var/lib/rancher/rke2/bin/ctr --address /run/k3s/containerd/containerd.sock --namespace k8s.io container ls
----

[,bash]
----
export CRI_CONFIG_FILE=/var/lib/rancher/rke2/agent/etc/crictl.yaml
/var/lib/rancher/rke2/bin/crictl ps
----
